# 시퀀스 맵

## 현재 레포

- `01. 요청/응답과 메모리 CRUD`

## 이전 레포

- `aandi-prerequisite-bootcamp`
- 이전 시퀀스: `00. 사전 준비 부트캠프`

## 다음 레포

- `spring-boot-db-access-lab`
- 다음 시퀀스: `02. 영속성 저장과 계층 분리`

## 학습 흐름

1. 사전 준비에서 HTTP, JSON, Git, DB 용어를 익힙니다.
2. 이 레포에서 Spring Boot 요청/응답과 메모리 CRUD를 구현합니다.
3. 다음 레포에서 MySQL과 JPA 기반 저장으로 넘어갑니다.

## 브랜치 이동 기준

1. 수업 시작 전에 `01-implementation`으로 이동합니다.
2. 구현과 테스트가 끝나면 `01-answer`와 diff를 비교합니다.
3. 가이드 문서 확인이 필요할 때만 `main`을 봅니다.
